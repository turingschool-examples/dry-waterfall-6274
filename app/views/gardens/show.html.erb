<h1><%= @garden.name %></h1>

<h2>Plants in the Garden that take less than 100 days to harvest</h2>
<% if @plants.present? %>
  <div>
    <% @plants.each do |plant| %>
      <li><%= plant.name %></li>
    <% end %>
  </div>
<% else %>
  <p>No plants found.</p>
<% end %>