<section class="plots">
  <% @plots.each do |plot| %>
    <div id="plot-<%= plot.id %>">
      <h3><%= plot.number %></h3>
      <% plot.plants.each do |plant| %>
        <div id="plant-<%= plant.id %>">
          <h2><%= plant.name %></h3>
          <%= button_to "remove plant from plot", "/plot_plants(#{plot.id}, #{plant.id})", method: :delete %>
        </div>
      <% end %>
    </div>
  <% end %>
</section>